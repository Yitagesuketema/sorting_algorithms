#include "sort.h"

/**
 * quick_sort_hoare - implementation of quick sort algorithm with the Hoare
 * partition scheme.
 * @array: array to be sorted
 * @size: size of the array
 */

void quick_sort_hoare(int *array, size_t size)
{
	print_array(array, size);
}
